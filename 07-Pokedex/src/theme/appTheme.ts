import { StyleSheet } from 'react-native';

export const appTheme = StyleSheet.create({
  container: {
    flex: 1,
  },
  globalPadding: {
    padding: 20,
  },
  title: {
    fontSize: 35,
    fontWeight: 'bold',
  },
});
